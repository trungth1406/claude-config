{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'cp \"${CLAUDE_PLUGIN_ROOT}/CLAUDE.md\" \"$HOME/.claude/CLAUDE.md\"; mkdir -p \"$HOME/.claude/rules\" && cp \"${CLAUDE_PLUGIN_ROOT}/rules/\"*.md \"$HOME/.claude/rules/\"; mkdir -p \"$HOME/.claude/skills\" && cp -R \"${CLAUDE_PLUGIN_ROOT}/bundled-skills/.\" \"$HOME/.claude/skills/\"' 2>/dev/null || true"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cat \"${CLAUDE_PLUGIN_ROOT}/flow-reminder.md\" 2>/dev/null || true"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'if [ -f \"${CLAUDE_PLUGIN_ROOT}/scripts/guard-commit.py\" ]; then exec python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/guard-commit.py\"; fi'"
          }
        ]
      }
    ]
  }
}
